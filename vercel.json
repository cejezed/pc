{
  "version": 2,
  "buildCommand": "cd apps/client && npm run build",
  "outputDirectory": "apps/client/dist",
  "installCommand": "npm install && npm install --prefix apps/client",
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
