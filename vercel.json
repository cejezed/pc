{
  "installCommand": "npm install --prefix apps/client",
  "buildCommand": "npm run build --prefix apps/client",
  "outputDirectory": "apps/client/dist",
  "framework": null,
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/meal-plans/generate-shopping-list",
      "destination": "/api/meal-plans/generate-shopping-list.ts"
    },
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
